# RLS Policies

All RLS policies are versioned in `packages/db/migrations/*_rls_policies.sql`.

Property-based tests for the visibility matrix will live in `apps/mobile/__tests__/rls.matrix.test.ts`
once Batch 1 lands (need a live test DB).

Reference: `docs/SPEC.md` §I.
