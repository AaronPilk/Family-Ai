/**
 * Tab entry for the family tree. The actual screen lives at
 * `apps/mobile/src/app/family/tree.tsx`; we re-export here so it gets its
 * own slot in the bottom tab bar while keeping the underlying component
 * in one place.
 */
export { default } from '../family/tree';
