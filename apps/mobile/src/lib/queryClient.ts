// Stub — real QueryClient wires back in Batch 1 (auth) when we start hitting Supabase.
// Kept as an empty module so existing imports have a stable path.
export const queryClient = null as unknown as never;
